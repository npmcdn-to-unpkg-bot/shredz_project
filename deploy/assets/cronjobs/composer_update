# Update composer to latest version nightly at 12am, and log the result.
#
# m h dom mon dow user  command
0 0 * * * root /usr/local/bin/composer self-update >> /var/www/default/shared/logs/cron.log 2>&1
